language: node_js
node_js:
  - 6

install:
  - npm install
  - npm install -g firebase-tools

script:
  - npm test
  
after_success:
  - firebase deploy --token ${FIREBASE_TOKEN} --project ${FIREBASE_PROJECT}
